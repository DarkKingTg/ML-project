# requirements.txt
# BehaveGuard Prompt Injection & Jailbreak Detection System

# Core ML / data
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Deep learning
torch>=2.0
transformers>=4.30       # required for the real "transformer" embeddings backend;
                          # the system runs without a real download via the
                          # "hashing" fallback backend (see features/transformer_embeddings.py)

# NLP preprocessing
nltk>=3.8

# Config
pyyaml>=6.0

# Plotting
matplotlib>=3.7

# Model persistence
joblib>=1.3

# API
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0

# Testing (optional, for running the test suite)
pytest>=7.4
httpx>=0.24               # required by fastapi.testclient.TestClient
